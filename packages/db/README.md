# `db`

```json
"scripts": {
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev --skip-generate",
    "db:deploy": "echo 'Add deploy script here'",
    "db:studio": "prisma studio"
  },
```
